
; initialize row
LOAD s0, 00
OUTPUT s0, 40
LOAD s0, 10
OUTPUT s0, 40

; initialize column
LOAD s0, 00
OUTPUT s0, 10
LOAD s0, 10
OUTPUT s0, 10

; draw a line
LOAD s0, 00
LOAD s1, 07
line:
OUTPUT s1, c1
SUB s0, 01
JUMP NZ, line

; hang
hang: JUMP hang
