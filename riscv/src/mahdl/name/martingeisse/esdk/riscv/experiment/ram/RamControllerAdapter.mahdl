native module name.martingeisse.esdk.riscv.experiment.ram.RamControllerAdapter;

interface {
    in clock clk;
    in bit enable, write;
    in vector[24] wordAddress;
    in vector[32] writeData;
    in vector[4] writeMask;
    out vector[32] readData;
    out bit acknowledge;
}
