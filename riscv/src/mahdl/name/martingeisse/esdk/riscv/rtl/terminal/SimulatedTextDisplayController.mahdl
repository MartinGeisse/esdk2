native module name.martingeisse.esdk.riscv.rtl.terminal.SimulatedTextDisplayController;

interface {
    in clock clk;
    in bit clockEnable;
    in bit writeEnable;
    in vector[12] address;
    in vector[8] writeData;
}
